<base:PageBase
    x:Class="SampleUwp.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:base="using:GeekyTool.Base"
    DataContext="{Binding Source={StaticResource Locator}, Path=MainViewModel}"
    mc:Ignorable="d">

    <Grid Background="#2c3e50">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Rectangle Margin="0,0,0,0" Fill="#FF202D3A"/>
        <TextBlock Grid.Row="0" TextWrapping="Wrap" Text="Sample UWP" Style="{StaticResource HeaderTextBlockStyle}" FontWeight="Bold" Foreground="#FF3498DB" Margin="12" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Margin="12,30,12,0" >
            <Button Content="Button" Style="{StaticResource ButtonSampleUwpStyle}"/>
            <Button Content="Button" Style="{StaticResource ButtonSampleUwpStyle}"/>
            <Button Content="Button" Style="{StaticResource ButtonSampleUwpStyle}"/>
            <Button Content="Button" Style="{StaticResource ButtonSampleUwpStyle}"/>
            <Button Content="Button" Style="{StaticResource ButtonSampleUwpStyle}"/>
            <Button Content="Button" Style="{StaticResource ButtonSampleUwpStyle}"/>
        </StackPanel>
    </Grid>
</base:PageBase>

